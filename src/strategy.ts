// Стратегический набор подключается только после проверки всех обязательных полей.
export type StrategySource={id:string;version:string;url:string;permission:string;verified:boolean;players:number;stackBB:number;blinds:[number,number];anteBB:number;rake:{percent:number;capBB:number};openToBB:Record<string,number>;category:'teaching'|'solver';solver?:{commit:string;treeHash:string;accuracy:number};hands:Record<string,{actions:Record<string,number>;ev?:Record<string,number>}>};
export const activeSource:StrategySource|null=null;
export const sourceReview={checked:'17.09.2026',name:'PokerCoaching · Online 6-max',url:'https://poker-coaching.s3.amazonaws.com/tools/preflop-charts/online-6max-gto-charts.pdf',status:'Не подключён',reason:'PDF содержит упрощённые таблицы для 6-max, 100 BB. Открытия: до 2,5 BB; SB — до 3 BB. Анте, комиссия и разрешение на встраивание не подтверждены. EV и исходные частоты не опубликованы. Бесплатный просмотр не означает разрешение на перенос.'};
